%table.table.table-striped
  %thead
    %tr
      %th Было
      %th Стало
      %th 

  %tbody
    - @jobs.each do |t|
      %tr
        %td= t.source[0..45]
        %td= t.output[0..45]
        %td= link_to 'Destroy', t, :remote => true, method: :delete, data: { confirm: 'Are you sure?' }